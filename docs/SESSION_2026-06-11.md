# HaxJobs Reset Session — 2026-06-11

## What was done today

### 1. Product anchor docs
- Created `docs/HAXJOBS_PRODUCT_SPEC.md` — the real product definition
- Created `docs/HAXJOBS_RESET_PLAN.md` — step-by-step reset plan
- Created `docs/REPO_MAP.md` — inventory of every file: active, legacy, generated, future automation
- Staged automation ladder: Level 0 (discovery) through Level 5 (full auto-apply for trusted flows)

### 2. Role taxonomy and family classifier
- Created `profile/role_taxonomy.json` — 7 CV variant families mapped to 40+ real scraped job titles
- Created `evaluation/role_family.py` — deterministic classifier, not LLM-based
- 18 tests for the classifier (real scraped title examples)
- Existing 113 scraped jobs were backfilled on Archilles with role family and CV variant

### 3. DB and API integration for role families
- Added SQLite fields: `role_family`, `role_family_confidence`, `recommended_cv_variant`, `source_quality`, `pack_status`, `outreach_status`, `classified_at`
- Additive migration that is safe for existing Archilles databases
- New jobs auto-classify on insert
- `/api/jobs` exposes the new fields
- Dashboard shows CV variant badges and detail cards
- Pipeline keeps roles fresh via `python3 pipeline_db.py classify-roles`
- 23 tests total for DB layer

### 4. Reusable CV variant registry
- Created `cv_variants/registry.json` — 7 stable CV variants
- Created `cv_variants/registry.py` — helpers for pack metadata
- Pack metadata contract: `pack_owns_cv: false`
- Per-job packs reference a reusable variant, they do not generate new CVs
- Fixed evaluator prompt in `evaluate_with_hermes.py` — it no longer says "generate CV"
- Seeded actual CV draft files from old packs via `scripts/seed-cv-variants-from-packs`
- Created `scripts/pull-cv-variants` — pull CV files from Archilles to Jade over SSH
- `.gitignore` prevents accidental CV PDF/HTML commits
- 7 tests for registry contract

### 5. Dashboard tunnel fix
- Created `scripts/dash-tunnel` — clean SSH tunnel for dashboard
- Installed as `dash-tunnel` command in `~/.local/bin/`
- Usage: `dash-tunnel` then visit http://localhost:5173

## Current state

- **Local**: `/home/hax/haxjobs` on branch `main`, HEAD `92d504e`, working tree clean
- **GitHub**: `haxsysgit/Haxjobs`, HEAD `92d504e`
- **Archilles**: `/home/hermes/haxjobs`, HEAD `92d504e`, API 200, Vite 200
- **Tests**: 31 passed
- **Scraped jobs**: 113 in DB, classified by role family
- **CV variants**: 7 draft variant PDFs on both Archilles and Jade

## Commits today

```
3406187 Add HaxJobs role taxonomy classifier
ac1381a Tune AI role taxonomy for scraped junior titles
5afed31 Document HaxJobs product reset
6d0d85f Map HaxJobs repo boundaries
369af9d Persist role families on jobs
58b7efb Add reusable CV variant registry
92d504e Keep promoted CV files private
```

## Important commands

```bash
# Pull latest CV variant files from Archilles to Jade
scripts/pull-cv-variants

# Open dashboard tunnel
dash-tunnel

# Update Archilles from GitHub
ssh archilles haxjobs-update

# Backfill role families on Archilles
ssh archilles "cd /home/hermes/haxjobs && python3 pipeline_db.py classify-roles"

# Run all tests
cd /home/hax/haxjobs && PYTHONPATH=. pytest -q
```

## Next steps (do not start until asked)

1. Review and polish the 7 draft CV variants, starting with `backend_python`
2. Build the actual per-job pack generator that uses role family + CV variant registry
3. Wire pack generation into the pipeline runner
4. Add LinkedIn discovery as a first-party pipeline source
5. Build LinkedIn networking/outreach module
6. Clean up legacy code (see `docs/REPO_MAP.md` for safe list)

## Guardrails

- No per-job CV generation — always use reusable CV variants
- Archilles only sends Telegram — Jade never sends
- GitHub is source of truth — Jade commits, Archilles pulls with `haxjobs-update`
- CV PDFs are private — `.gitignore` blocks them
- Tests before code — 31 tests currently passing
- Automation is staged — do not skip to unsafe auto-apply
