# HaxJobs draw.io diagrams

These are actual draw.io diagrams, not Mermaid-only source files.

## Main files

### 1. Actual work pipeline

Editable draw.io source:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/01-haxjobs-actual-work-pipeline.drawio
```

PNG preview:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/01-haxjobs-actual-work-pipeline.png
```

Editable PNG with embedded draw.io XML:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/01-haxjobs-actual-work-pipeline.drawio.png
```

Purpose: shows the clean end-to-end HaxJobs workflow from job source to human-approved action.

### 2. Output pack

Editable draw.io source:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/02-haxjobs-output-pack.drawio
```

PNG preview:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/02-haxjobs-output-pack.png
```

Editable PNG with embedded draw.io XML:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/02-haxjobs-output-pack.drawio.png
```

Purpose: shows the actual files HaxJobs should produce for one approved job.

### 3. Stage debug checklist

Editable draw.io source:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/03-haxjobs-stage-debug-checklist.drawio
```

PNG preview:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/03-haxjobs-stage-debug-checklist.png
```

Editable PNG with embedded draw.io XML:

```text
/home/hax/haxjobs-private-dev/docs/diagrams/03-haxjobs-stage-debug-checklist.drawio.png
```

Purpose: gives a clean checklist for debugging where a job is stuck.

## Open in draw.io

Open any `.drawio` file directly with draw.io desktop or diagrams.net.

On this machine:

```bash
drawio /home/hax/haxjobs-private-dev/docs/diagrams/01-haxjobs-actual-work-pipeline.drawio
```

Or open the PNG previews from the file manager.

## Important note

Older `.mmd` files in this folder are Mermaid sources. They are not the main deliverable. Use the `.drawio` files above.
